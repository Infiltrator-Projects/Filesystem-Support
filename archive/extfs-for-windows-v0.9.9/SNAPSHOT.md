# Frozen ExtFS-for-Windows v0.9.9 source snapshot

This directory is a preservation snapshot of `Infiltrator-Projects/ExtFS-for-Windows`
at commit `dda219be47033372217149bce8c8ee20f19ded0a` (tree
`76b127deaa8b5b4776774a9579ca6c791d9d6b38`).

It is **not production source and must never be added to a build**. It exists only
while the Windows driver/build/test logic is migrated into Filesystem Support and
the duplicate EXT filesystem core is reconciled into the canonical filesystem
implementation.

The original repository's `third_party/infiltratr-common` gitlink pointed to
`748e089ae175329471d4cf375522c44081371bd5`; gitlinks are recorded here rather
than copied as source.

Delete this snapshot only after the migration preservation ledger's final
forensic comparison has passed.
