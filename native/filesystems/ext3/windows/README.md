# EXT3 Windows adapter

This directory is reserved for the EXT3 Windows IFS/WDK adapter.

No Windows EXT3 driver is implemented here yet. When Windows support is added,
it must consume the canonical EXT3 core rather than become a second filesystem
implementation.
