# EXT4 Windows adapter

This directory is reserved for the EXT4 Windows IFS/WDK adapter.

No Windows EXT4 driver is implemented here yet. When Windows support is added,
it must consume the canonical EXT4 core rather than become a second filesystem
implementation.
