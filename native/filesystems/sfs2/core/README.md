# Canonical SFS2 core

This directory owns **SFS2 (SFS\\2, structure version 4)** semantics.

The implementation is being migrated from the real SFS2-capable source retained
under `../reference/`. SFS2's different object layout, large-file encoding
and extent-node representation are independently owned here.
